# Legacy Static Dashboard

This folder preserves the previous static/PHP implementation for reference.

The active project at the repository root is now a React/Vite app intended for
integration into IgniteHex or another modern frontend platform.

Do not commit live API keys, passwords, or private tokens here. Connect real
services through backend environment variables or the host platform's secret
manager.
